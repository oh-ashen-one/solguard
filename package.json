{
  "name": "solguard",
  "version": "0.1.0",
  "private": true,
  "description": "AI-Powered Smart Contract Auditor for Solana",
  "repository": {
    "type": "git",
    "url": "https://github.com/oh-ashen-one/solguard"
  },
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "cli": "pnpm --filter @solguard/cli",
    "web": "pnpm --filter @solguard/web",
    "dev": "pnpm --filter @solguard/cli dev",
    "build": "pnpm -r build",
    "test": "pnpm -r test"
  },
  "devDependencies": {
    "typescript": "^5.3.3"
  },
  "engines": {
    "node": ">=18"
  }
}
